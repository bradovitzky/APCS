package karel;

import kareltherobot.*;

public class Filler extends Robot{
	
	public Filler(int street, int avenue, Direction dir, int beeps)
	{
		super(street, avenue, dir, beeps);
	}
	
	public void turnRight()
	{
		turnLeft();
		turnLeft();
		turnLeft();
	}
	
	public void findCorner()
	{
		while (!frontIsClear())
		{
			turnLeft();
			if (frontIsClear())
			{
				move();
				turnRight();
			}
			else
			{
				turnLeft();
			}
		}
	}
	public void jumpHurdle()
	{
		findCorner();
		move();
		turnRight();
		findCorner();
		move();
		turnRight();
		findCorner();
	}
	public static void main(String[] args) 
	{
		MakeHurdles temp = new MakeHurdles();
		World.readWorld("hurdlesx.kwld");
		World.setVisible(true);

		Filler Frank = new Filler(1, 1, East, infinity);
		World.setDelay(1);
		
		// pre: Frank is doing street one, starting at the origin facing East
		// post: Frank has finished the first row, and is sitting on top of the beeper on street 1
		while(Frank.frontIsClear() && !Frank.nextToABeeper())
		{
			Frank.putBeeper();
			Frank.move();
		}
		Frank.jumpHurdle();
		
	}

}
