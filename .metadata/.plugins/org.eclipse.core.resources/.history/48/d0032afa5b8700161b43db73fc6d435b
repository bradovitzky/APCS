package fracCalc2;
import java.util.*;
public class FractionalCalculator
{

	public static void main(String[] args)
	{
		Scanner console = new Scanner(System.in);
		System.out.println("Welcome to the Fraction Calculator!");
		System.out.println("Enter an expression (or \"quit\"): ");
		String input = console.nextLine();
		while(!input.equals("quit"))
		{
			convertToFraction(input);
			System.out.println("Left operand: 1_1/2");
			System.out.println("Operator: +");
			System.out.println("Right operand: 3/4");
			System.out.println();
			System.out.println("Enter an expression (or \"quit\"): ");
			input = console.nextLine();
		}
		System.out.println("Goodbye!");
	}
	public static int getWhole(String input)
	{
		
		
		if(underscoreIndex != -1)
		{
			return 0;
		}
		else
		{
			return Integer.parseInt(input.substring(0, underscoreIndex));
		}
	}
	
	public static int getNumerator(String input, int number)
	{
		int slashIndex = input.indexOf("/", number * 2); // This will check if its the first fraction or the second
		
		return Integer.parseInt(input.substring(, endIndex));
	}
	
	public static void getDenominator(String input, int number)
	{
		int slashIndex = input.indexOf("/", number * 2);
	}
	public static String findOperator(String input)
	{
		
		return null;
	}
	public static String convertToFraction(String input)
	{
		int underscoreIndex = input.indexOf("_");
		
		int spaceIndex = input.indexOf(" "); // To get the next one, use spaceIndex = input.indexOf(" ", spaceIndex + 1);
		getWhole(input);
		int number = 0;
		int numerator;
		int denominator;
		int operator;
		String answer;
		
		else
		{
			numerator = Integer.parseInt(input.substring(0, slashIndex));
		}
		denominator = Integer.parseInt(input.substring(slashIndex));
		System.out.println(numerator + "/" + denominator);
		
		return "All done!";
	}

}
